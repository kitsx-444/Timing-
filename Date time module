# A trader wants to know the time a major session opens in their local time. What the hell, this is an
# actual program I could use genuinely; great exercise Claude.

# - `striptime` is for reading dates.
# - `timedelta` is for changing dates

# First create the dictionary.
# Next, create the input variables.
# try:
# Then I have to convert the values into Python objects. I will use strptime within the function to convert the dictionary inside a loop and assign them
# their own variables.
# Within the function I will create a variable named hour, and in it use timedelta(hour=input)
# I will calculate the session times and print them.
# except: the likely error will be receiving a string and not a number. A value error is most likely.
# I will call the function.

from datetime import datetime, timedelta

time = {'London': '08:00',
		'New York': '13:00',
		'Tokyo': '00:00',
		'Sydney': '22:00'}

def convert_sessions(time, user_time):
		time_format = '%H:%M'
		hour = timedelta(hours=user_time)

		for session, open_time in time.items():
			# session = 'London'
			# open_time = '08:00' | This is called tuple unpacking
			parsed = datetime.strptime(open_time, time_format)
			#datetime.strptime converts the string into an object.
			#open_time is the string being converted
			#time_format tells python what to expect
			#Take the string '08:00', read it as hours and minutes,
			# and store it as a Python datetime object called parsed
			local = parsed + hour
			print(f'{session}: {local.strftime(time_format)}') #converts local into text, with the way python should read it in brackets.
try:
	user_time = int(input('What is your UTC offset: '))

except (ValueError, NameError):
	print('Input will only accept UTC offsets')
	exit()

convert_sessions(time, user_time)
